{
  "name": "puzzle-craft-forge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && npm run start",
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
} 